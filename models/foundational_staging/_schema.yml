version: 2

groups:
  - name: foundation_team
    owner:
      name: Foundation Team
      email: foundation_team@example.com

models:
  - name: foundation_stg_model
    description: "An staging model in the foundation project"
    group: foundation_team
    access: private
    columns:
        - name: id
          description: "The primary key for this table"
          tests:
              - unique
              - not_null
